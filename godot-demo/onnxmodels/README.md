Place downloaded / synced viseme ONNX checkpoints under
`res://addons/vizeme-onnxmodels/` (see that README). The legacy `onnxmodels/`
folder is obsolete for runtime — use:

```bash
bash scripts/sync_vizeme_onnxmodels.sh
```
