# Vendored from https://github.com/goatchurchprime/lipsync (MIT)

Copied for the vizemes quality UI 3D panel only (no OVRLipSync / two-voip).

- `readyplayerme_avatar.glb` + textures — Ready Player Me head with `viseme_*` morphs
- Driver in repo: `res://viseme_face.gd` (slim `set_visemes` API)

Pin: see git history of this directory / upstream `main` at copy time.
