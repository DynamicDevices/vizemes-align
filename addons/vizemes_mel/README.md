# vizemes_mel (ship unit)

Godot 4.6+ GDExtension: `MelFrontend` — PCM → flattened mel contexts for `OnnxLoader`.

This folder is the **distributable** only (`.gdextension` + `bin/`). Sources and build live in `../../gdextension/`.

```bash
cd gdextension && scons platform=linux target=template_debug
```
