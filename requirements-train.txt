# Train / mel tensor stack (heavy — not installed by nix develop / CI).
#   python3 -m venv .venv && .venv/bin/pip install -r requirements-train.txt
-r requirements.txt
soundfile>=0.12
torch>=2.1
torchaudio>=2.1
onnx>=1.16
onnxscript>=0.1
onnxruntime>=1.17
